<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Issue Certificate</title>
    <link rel="icon" href="/favicon.ico" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="styles/style.css" />
    <link rel="stylesheet" href="styles/issueStyle.css" />
  </head>

  <body>
    <div class="header">
      <a href="/" class="logo">Certificate DApp</a>
      <div class="header-right">
        <a href="/">Home</a>
        <a href="/issue">Issue Certificate</a>
        <a class="active" href="/records">View Records</a>
      </div>
    </div>
    <br />
    <div class="entry-form">
      <h3>View Records</h3>
      <form
        class="d-flex justify-content-start align-items-center"
        id="needs-validation"
        action="/records"
        method="POST"
      >
        <select class="form-control" name="course" required>
          <option value="" disabled selected hidden>Select Course</option>
          <option value="Certified Blockchain Associate">
            Certified Blockchain Associate
          </option>
          <option value="Certified Ethereum Developer">
            Certified Ethereum Developer
          </option>
          <option value="Blockchain Foundation">Blockchain Foundation</option>
          <option value="Ethereum Fundamentals">Ethereum Fundamentals</option>
        </select>

        <button class="btn btn-info ms-4" type="submit">View</button>
      </form>
      <div class="mt-4">
        <% if (data.length > 0) { %>
        <h6>Certificates issued in <%= course %>: <%= data.length %></h6>
        <table class="table table-bordered mt-4">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">ID</th>
              <th scope="col">Grade</th>
            </tr>
          </thead>
          <tbody>
            <% for (var i = 0; i < data.length; i++ ) { %>
            <tr>
              <th scope="row"><%= i+1 %></th>
              <td><%= data[i]["id"] %></td>
              <td><%= data[i]["grade"] %></td>
            </tr>
            <% } %>
          </tbody>
        </table>
        <%} else { %>
        <h6><%= message %></h6>
        <% } %>
      </div>
    </div>
  </body>
</html>
